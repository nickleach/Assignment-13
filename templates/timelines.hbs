
<ul class"ul-right">
  {{#each timelines}}
      <div class="dates">{{date}}</div>
      <div class="times">{{times}}</div>
      <li class="icons"><i class="fa {{icon}}"></i></li>
      <li class="mainstuff">
      <h4>{{title}}</h4>
      <section>{{content}}</section>
      </li>
  {{/each}}
</ul>
